# Built frontend. Generated by `make build`; never committed, so the repo
# cannot drift from what the build actually produces.
ui-src/dist/
imswitch_plugin_example/ui/dist/

# Deployable tree, produced by `make dist`.
dist/
build/

node_modules/
__pycache__/
*.pyc
*.egg-info/
.venv/
